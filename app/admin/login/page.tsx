import AdminLogin from '@/components/AdminLogin';

export const metadata = {
  title: "Panel de Administración | Natural's Pharma",
  description: 'Acceso seguro para la gestión de inventario.',
};

export default function AdminLoginPage() {
  return <AdminLogin />;
}